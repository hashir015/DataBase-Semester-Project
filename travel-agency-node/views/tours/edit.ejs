<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Tour</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1 class="mb-4">Edit Tour</h1>
        <form action="/tours/edit/<%= tour.TourID %>" method="POST" class="card p-4 shadow">
            <div class="mb-3">
                <label for="Tour_Name" class="form-label">Tour Name</label>
                <input type="text" class="form-control" id="Tour_Name" name="Tour_Name" value="<%= tour.Tour_Name %>" required>
            </div>
            <div class="mb-3">
                <label for="Schedule" class="form-label">Schedule</label>
                <input type="datetime-local" class="form-control" id="Schedule" name="Schedule" value="<%= new Date(tour.Schedule).toISOString().slice(0, 16) %>" required>
            </div>
            <div class="mb-3">
                <label for="Tour_Location" class="form-label">Location</label>
                <input type="text" class="form-control" id="Tour_Location" name="Tour_Location" value="<%= tour.Tour_Location %>" required>
            </div>
            <div class="mb-3">
                <label for="Price" class="form-label">Price</label>
                <input type="number" class="form-control" id="Price" name="Price" value="<%= tour.Price %>" required>
            </div>
            <button type="submit" class="btn btn-primary">Update Tour</button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
